nav.navbar.navbar-default.navbar-fixed-top
  .container
    .navbar-header
      a.navbar-brand(ui-sref="home") Event Reporter
      button.navbar-toggle(ng-click="isCollapsed = !isCollapsed")
        span.sr-only Toggle Navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
    .navbar-collapse.navbar-right(collapse="isCollapsed")
      ul.navbar-nav.nav
        li.dropdown(dropdown="", ng-show="LoginService.session.userid !== null")
          a.dropdown-toggle(
            href="",
            dropdown-toggle="") {{LoginService.session.userid}}
            span.caret
          ul.dropdown-menu(role="menu")
            li
              a(ui-sref="user") View user page
            li
              a(ui-sref="editProfile") Edit profile
            li
              a(ng-click="LoginService.logout()") Sign out
        li(ng-show="LoginService.session.userid == null")
          a(ng-click="LoginService.login()") Sign in
